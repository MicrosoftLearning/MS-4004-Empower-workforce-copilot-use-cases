## Summary

Updates Exercise 2, Task 1 of Module 01 (Empower the executive workforce) so the Copilot in Word walkthrough matches the current Microsoft 365 Copilot UI, and applies Microsoft Learn style conventions to the task content.

## Changes

### M01 — Exercise 2, Task 1: Use Copilot in Word to create an executive briefing report

- **UI drift fixes:**
  - Updated the Microsoft 365 sign-in URL from `https://www.microsoft365.com` to `https://m365.cloud.microsoft.com`.
  - Added a fallback note explaining how to reach Word via the App Launcher when **Apps** isn't visible in the navigation pane.
  - Rewrote step 4 to open Copilot from the in-document **Copilot** icon (bottom-right) and to verify the pane is in **Allow editing** mode, replacing the outdated **Edit with Copilot** plus-menu flow on the Home ribbon.
  - Updated step 9 to reflect that Copilot now inserts the Q4 Strategic Recommendations section directly into the document rather than requiring the learner to copy and paste the response.
  - Reworded step 3 to `select **Create blank document**` to match the current Word for the web start screen.
- **Style fixes:**
  - Replaced the stray `---` horizontal rule that followed the H1 with a blank line.
  - Promoted the `#### Using Copilot in Word` heading to `##` so it sits at the correct level in the task hierarchy.
  - Normalized numbered-list spacing (single space after the period) across all 14 steps.
  - Wrapped the typed value `Northwind` and the long sample COO prompt in backticks per the style guide for literal input.
  - Smoothed minor wording (for example, *"Place your cursor at the start of the document and paste the copied content"*).

## Files changed

- `Instructions/Labs/M01-empower-workforce-copilot-executives/08-Ex2-1-use-word-create-executive-briefing.md` — UI drift fixes for the Word + Copilot flow (URL, App Launcher fallback, new Copilot pane entry point, direct-edit behavior) and Microsoft Learn style cleanup (heading levels, list spacing, backticks on typed input and sample prompt).
